# 리팩토링 중 발견한 관찰/버그 후보 (2026-06-12, 트리아지 2026-06-22)

> 리팩토링 phase 1~4 진행 중 에이전트들이 발견. **원칙에 따라 수정하지 않고 기록만** — 리팩토링 커밋과 동작 변경을 분리하기 위함. 각 건은 유저 판단 후 별도 수정.

## 버그 후보 — 2026-06-22 트리아지: #1 FIXED, #2/#3 OPEN, #4 의도된 동작(주석화). 2026-07-11: #2/#3 FIXED

1. **[심각] OCO 생성 크래시 윈도우** — `createOcoList` tx 커밋 후 limit leg NO 발행 전 크래시 시, 부트 복구(`recoverList`)에 "EXECUTING + 양 레그 NEW + stopPendingAt null" 분기가 없어 limit NO가 영원히 미전송 → 잠금 자금 고착. 이후 stop 트리거 시 엔진-unknown limit에 CO 발행. (order-list)
   - **✅ FIXED (2026-06-22)**: 엔진 멱등성(`OrderBook.contains` + `submit_new_order`가 이미 resting인 id의 중복 NEW 무시) + `recoverList` 크래시윈도우 분기(limit NO 재드라이브, stop 재arm은 trigger.service:38이 담당). 엔진은 정의상 미체결 limit만 대상이라 안전. 테스트: matcher dedup + recoverList 크래시윈도우. pytest 44/44, jest order-list 27/27. **부수효과: 기존 복구 분기들(stop NO 재전송 등)의 이중전송 리스크도 제거**.
2. **OCO 취소 vs arming 레이스** — arming claim 커밋~NO emit 사이에 cancelList가 CO를 먼저 emit하면 파티션에서 CO가 NO를 앞질러 유저 취소가 소실된 채 stop 레그가 엔진에 잔존. (order-list)
   - ~~**VERIFIED STILL-PRESENT**: `order-list.service.ts:318-327,364-379`. 코드 주석(line 369 "레이스 패배")이 인지하나 방어 CO는 역방향만 보호. kafkajs `send()`는 호출 간 순서보장 없음([kafka.service.ts:29-35]).~~
   - **✅ FIXED (2026-07-11)**: arming 경로가 NO ack **후** `cancelRequested`를 재확인하고 추격 CO 전송(`redriveCancelIfRequested` — NO ack 뒤 전송이라 파티션 순서 보장, 중복 CO는 엔진이 unknown 무시). 부트 복구에 cancelRequested 재드라이브 분기 추가: 미트리거=로컬 취소, armed NEW=NO 재드라이브(엔진 멱등) 후 CO, 엔진 거주=CO. 테스트: 추격 CO 인터리빙 + 복구 3분기. jest order-list 34/34.
3. **trigger NO 전송 실패 dead path** — fire 실패 시 registry는 복원되지만 triggeredAt claim이 박혀 있어 다음 trade 재발화가 무전송. armed-but-unsent 상태가 다음 부팅 복구까지 지속. (trigger)
   - ~~**VERIFIED STILL-PRESENT (알려진 한계)**: `trigger.service.ts:65,71-89,112-127`. spec:344-360이 "세션 내 재발화 무전송, 부트 복구 의존"으로 명시. catch가 DB triggeredAt claim을 롤백 못 함.~~
   - **✅ FIXED (2026-07-11)**: 단일 주문 — claim 유지한 채 5s 주기 재전송 루프(`redriveArmedNo`, 매회 DB status 재확인으로 이중전송 가드 — 부트 복구와 동일 패턴; status가 NEW를 벗어나면 중단). OCO 레그 — 발화 실패 마커(`ocoRedrive`)로 다음 trade에서 가격 조건 없이 재발화하고, claim 선점 상태의 유실 CO/NO를 `onStopTriggered(leg, redrive=true)`가 재드라이브. jest trigger 21/21, 전체 289/289.
4. **상장 직후 검증 우회** — last price 부재 시 stop 즉시-트리거 검사와 market-like SELL minNotional 검사가 생략됨. (order-validation)
   - **의도된 동작으로 결론 (2026-06-22, 주석 명확화함)**: #4a stop 즉시트리거는 *체결가* 기준이라 거래 0건이면 트리거 불가→skip이 정상(버그 아님). #4b market SELL minNotional은 lastPrice 없으면 추정 불가→placement skip은 의도적(빈 책이면 미체결, dust는 정산 처리). 무리한 reject는 정상 첫 주문 과잉차단이라 미수정. `order.service.ts`·`order-validation.ts` 주석에 의도 명시.

## 일관성/설계 관찰 (현재 무해)

5. **ROUND_DOWN vs ROUND_FLOOR** — `settlement.service.ts` 수수료 절사가 ROUND_DOWN(0방향), shared floor8는 ROUND_FLOOR(−∞방향). 피연산자가 실질 양수라 결과 동일하나 음수에서 1비트 다른 함수. 통일 여부 판단 필요.
6. **OCO 0원 환불 멱등성** — 전량 체결 finalize 시 listref 이벤트 row가 안 생겨 멱등성이 sourceKey가 아닌 status claim에만 의존.
7. **liquidation restoreNormal** — count===0(worker가 먼저 복귀)에도 "released" 로그 출력.
8. **LIQUIDATING 순간 노출** — claim 직후 재검증 통과 시 fuser로 LIQUIDATING→NORMAL이 연달아 emit.
9. **futures placeOrder 검증 순서** — 필드 normalize가 LIQUIDATING 체크보다 먼저 실행(주석의 순서 표기와 미세 불일치, 동작 영향 없음).
10. **DEFAULT_LEVERAGE=10 3중 동기화** — Prisma 스키마 기본값 / BE futures-trading / FE 주문 폼이 수동 동기화 전제.
11. **partitionOf 캐시 중복** — FuturesTradingService와 LiquidationExecutor가 각자 보유.
12. **cancel-replace 비원자** — 신규 placement 후 cancelSingle 실패 시 두 주문·두 잠금 공존(코드 주석에 인지된 동작).
13. **cancelSingle의 OCO dust 가드 도달 불가** — 모든 호출 경로가 사전 라우팅. 방어 코드.
14. **stop registry.add가 tx 커밋 후** — 커밋~add 사이 크래시 시 rehydrate까지 트리거 평가 누락(부트 복구로 회복됨).
15. **spot/futures API 필드명 불일치** — 요청 DTO가 spot `tickerSymbol`/futures `symbol`. FE 통일은 BE DTO 변경(계약 변경)이 선행돼야 해서 보류.
16. **FE lib/hooks 시장별 훅 중복** — `useTrades` vs `useFuturesTrades` 등 react-query 훅 쌍이 잔존(Phase 4 승인 범위 외). 통합 시 queryKey 체계 재설계 필요.
17. **단일 stop 취소 vs 발화 NO 앞지름 (2026-07-11 추가)** — `cancelSingle`이 armed stop에 보내는 CO가 발화/재전송 NO보다 먼저 파티션에 닿으면 엔진이 무시(#2와 동형이나 Order에는 cancelRequested 필드가 없어 같은 패턴 적용 불가). NO가 자리잡은 뒤(OPEN) 유저가 재취소하면 해결되고, #3 수정으로 armed-but-unsent 고착이 사라져 윈도우가 좁아짐. 근본 해결은 Order에 취소 의도 영속화(예: `cancelRequestedAt`) 후 #2 패턴 적용 — 스키마 변경이라 유저 판단. (order)
18. **[심각·해결 2026-07-12 ADR-067] 정산 워커 poison 이벤트가 FIFO 드레인 영구 차단 (2026-07-12, 첫 미러링 실행에서 검출)** — `FuturesSettlementWorker`가 적용 불가 이벤트 하나(관측된 트리거: `futures USDT wallet not found`)에서 throw하면 FIFO 순서상 뒤 이벤트가 전부 PENDING 고착, 매 tick 무한 재시도(정산 파이프라인 정지). poison 이벤트 격리/스킵/DLQ/quarantine 부재. 실제로 130건이 고착됐고 F1c(exec=Σ체결)·F4(locked) 불일치로 하네스가 자동 검출. 방아쇠는 dev 크로스마켓 충돌(test-report 2026-07-12 F#1, up.sh 2-인스턴스 분리로 수정)이었으나 **임의의 적용 불가 이벤트가 전체 정산을 멈추는 로버스트니스 갭은 프로드에서도 위험**. spot `SettlementWorker`도 동형 여부 검토 필요. 근본 해결: 실패 이벤트 N회 후 격리(별도 status/DLQ) + 알람, 후속 이벤트 진행. (settlement)
   - **✅ 해결 (2026-07-12, ADR-067 DLQ)**: 이벤트별 연속 실패 5회면 `QUARANTINED` + `SettlementDeadLetter` 사본으로 격리하고 후속 이벤트를 진행한다. 격리 발생은 error 로그와 정합성 F5로 드러난다. 격리 이벤트 재적용 도구는 아직 없음(감사 A15). 아래는 발견 당시 기록.
20. **[아키텍처 갭] FUTURES 단독 상장은 거래 불가 — mark price가 로컬 SPOT 체결에만 의존 (2026-07-12, 신생 알트 상장 실험 검출)** — 재시작-프리 상장으로 SLXUSDT를 FUTURES에만 상장하자 모든 주문이 `mark price unavailable: spot index not established`로 거절. mark-price 서비스가 같은 심볼의 로컬 spot 체결 스트림에서만 인덱스를 만들고 외부 오라클이 없어서, spot 시장이 없는 선물 상장은 영원히 거래 불가. 실거래소는 외부 인덱스(복수 거래소 가중)를 쓴다. 해결 방향(정책 결정 필요): ① 선물 상장 시 spot 동시 상장 강제, ② 미러 소스(Binance mark)를 인덱스로 주입하는 오라클 경로, ③ 인덱스 부재 시 상장 API가 명시 거부. agents CLI의 관련 안내 문구는 갱신함. (mark-price / admin-market)

21. **[심각·해결 2026-07-16~08-24 ADR-069·F0] BE 재기동이 정산 이벤트/언락을 유실 — 드레인 후에도 잔존하는 monetary fail (2026-07-14, 라이브 재기동 사고에서 실측)** — 10:14 BE watch-재기동(다른 에이전트의 libs 저장이 트리거) 시 컨슈머 그룹 리밸런스(10:19–10:20 하트비트 에러) 후, 전 주문흐름 정지+드레인 상태에서 정합성 체크에 잔존 fail: ① **F1c 2건** — `b12d141c` exec=0인데 fills=0.0001 (Trade는 기록, 주문 executedQty 반영 유실), `6febe11b` exec=0.14466 vs fills=0.14476. ② **F4 frozen leak 2건(gross)** — `136401b6` 열린주문 0인데 DEXE 4.45 영구 동결, `fa197996` +1.19 과다 동결 (에이전트 서브계정 실피해). 전일 밤 동일 체크는 클린 PASS였으므로 오늘 아침 재기동들이 원인. 리밸런스 순간 오프셋 커밋 vs DB 반영의 원자성 부재 추정 — settlement worker의 sourceKey 멱등 처리가 "이미 처리됨" 오판으로 진짜 반영을 스킵했을 가능성. **재현 경로: 활발한 정산 흐름 중 be-spot 재기동.** 근본 해결은 컨슈머 offset commit과 DB 반영의 순서/원자성 감사 (settlement worker 오너십: 현재 다른 에이전트 작업 중이라 증거만 인계). (settlement / kafka consumer)
   - **✅ 해결**: 원인이던 '오프셋 커밋과 DB 반영 사이의 비원자성 + 셧다운 시퀀스 부재'는 ADR-069 S2(저널 커밋 후 응답, 부팅 시 저널 리플레이로 복원, 2026-07-16)와 F0 그레이스풀 셧다운(2026-08-24, ADR-077 §1)으로 닫혔다. 이후 kill -9 리플레이 드릴 22,235건·31,999건 대조에서 불일치 0. `--watch` 실행은 금지(AGENTS.md How to run ⑤). 아래는 사고 당시 기록.

22. **[심각·부분해결] 재기동 후 엔진 레인 무반응 + 미러 봇 침묵 웨지 (2026-07-14 동일 사고)** — ZECUSDT(파티션 1 단독) 레인이 BE 재기동 후 소비는 지속(lane.last_offset 전진)하나 신규 NO를 사실상 처리 안 함 (seq 25분간 +33, out-토픽 ZEC 무응답, LIMIT IOC NO들이 무반응 → DB NEW 1,679건 자금 락 적체). 정확한 스킵 메커니즘은 엔진 pytest 재현 진행 중 (main.py 오프셋 가드 / restore 의미론 / 중복 id 경로가 용의선상). 부속 병리: **bots fetch 무타임아웃** → BE 순단에 요청 영구 행잉 = maker:ZEC 루프 침묵 사망 + 재소생한 미러도 응답 못 받으며 place→cancel 무한 루프로 CO 스팸(파티션 1 트래픽 100:1) 유발. **✅ bots는 수정**: exchange.ts 3개 fetch에 `AbortSignal.timeout(10s)` (2026-07-14, typecheck 통과). 엔진 수정은 재현 테스트 결과에 따름. (match engine / bots)

23. **[심각·해결 2026-07-16 ADR-069 S2 → 후속 병목 #26] BE 처리량 붕괴 — Wallet 행 락 컨보이→풀 고갈 캐스케이드 × lastFillOf 무인덱스 O(백로그) 스캔 (2026-07-14 14:14–14:29 라이브 실측, EVO-9 캠페인 중)** — 지속 유입이 상한(실측 60.8 TPS 균형, 피크 75)을 넘자 처리량이 2.4~15 TPS로 20~30배 붕괴, NEW 백로그 8,000 폭주. 유입 차단(미러 SIGINT) 즉시 63~72 TPS 회복, 92초 드레인. **병리 2종의 곱**:
   - **✅ 해결(구조)**: Wallet 행 락 컨보이는 ADR-069 S2(인메모리 원장 + 저널, Wallet 행은 프로젝션)로 소멸했고 lastFillOf 인덱스는 핫픽스로 적용됐다. 남은 천장은 Node 이벤트루프 단일 코어(#26)이며 M1 정산 프로세스 분리(ADR-077)로 옮겼다. 아래 실측 기록은 이력.
   - **(주범) 자산락 직렬화 + 풀 태우기**: 주문접수 tx(`order.service.ts:239-306`)가 wallet.updateMany(행 락) 획득 후 3 statement+commit 동안 보유. 미러 메이커/테이커가 각 단일 계정이라 8심볼 전 주문의 동결/정산/환불이 USDT Wallet 극소수 행으로 직렬화(실측: Wallet 132행 n_tup_upd 660만). Prisma 인터랙티브 tx는 **락 대기 중에도 커넥션 점유** → 유입 초과 순간 락 대기자들이 풀(기본 17 = cpu×2+1)을 포화 → 정산 컨슈머·워커·kline·에이전트 폴링·주문접수 전원 동반 붕괴 (유입 insert도 3,500→600/min). 직렬 상한 추정 60~150 TPS가 실측과 부합 — **상한의 정체는 DB가 아니라 단일 계정 MM 구조**.
   - **(공범) lastFillOf O(백로그)**: `match-event-orchestrator.ts:165-193`이 Trade를 maker/takerOrderId로 조회하는데 **해당 인덱스 부재** → seq 역스캔이 백로그에 비례해 비싸짐 (EXPLAIN 실측 최악 728ms, `Trade_seq_key` 누적 93억 tuple read/10.9만 스캔). 컨슈머는 `partitionsConsumedConcurrently` 미설정=직렬이라 90ms/msg면 상한 ~11 TPS → 붕괴 시 자기강화 되먹임. be-spot CPU 142%의 정체 = 이 스캔(pg측) + 정산 워커 Decimal/Prisma 마샬링.
   - 기각: 데드락(pg deadlocks=0, 락 순서 일관), bots 재시도 폭풍(타임아웃은 재시도 안 함), 엔진(붕괴 중 CPU 2.6% 유휴). 셧다운 드레인이 빨랐던 이유 = cancelAllSpot가 bulk API (REST 경로에 wallet tx 없음).
   - **수정 플랜 (효과순, 미구현)**: ① MM 계정 심볼당 분리 — 봇 설정만, BE 무중단, 핫 로우 8배 샤딩 (정합성 체커 F1 대사 수정 필요) ② Trade (makerOrderId)/(takerOrderId) 인덱스 — 되먹임 절단 (schema.prisma는 타 에이전트 미커밋과 조율) ③ place tx 슬림화 (락 홀드 4→2 stmt) ④ 행별 세마포어 백프레셔 (락 대기를 풀 밖으로 = 절벽 봉인) ⑤ 워커 (user,asset) 델타 배치 ⑥ connection_limit 상향(보험) ⑦ 컨슈머 병렬화(②이후에만).
   - **✅ 락 컨보이 실측 확증 + 인덱스 부분 수정 (2026-07-14 15:15~15:40 합성 부하테스트)**: 전용 부하 생성기(`bots/src/loadtest.ts`, 신규 — 계정 N·토큰버킷 TPS·place/cancel+self-cross·p95 계측)로 계정 16 분산 100/200/300 TPS 램프. pg_locks 캡처 108건 — BLOCKED 전원이 `UPDATE "Wallet" SET balance=…, locked=…`(동결), BLOCKER는 **idle in transaction**(직전 쿼리 Wallet SELECT/Order INSERT) = 인터랙티브 place tx가 락 보유 채 라운드트립하는 현행범. **인덱스 핫픽스 적용** (`Trade_{maker,taker}OrderId_hotfix_idx` CONCURRENTLY + schema.prisma @@index 2줄, lastFillOf 728ms→1.3ms 실측; 마이그레이션은 유저 실행 대기). 결과: 접수 상한 ~200 TPS(계정 분산+인덱스, 오전 단일계정 60~75의 ~3배), 단 100 TPS부터 p95 스파이크(8.6s)·200부터 p50 1.5~2s·300 목표 시 달성 90~212 진동+에러 0.3%. **부하 중 정산은 여전히 기아(전이 ~1/s, stuck NEW 4k)**이나 부하 종료 즉시 20~30s 드레인 — 죽음의 되먹임은 인덱스로 절단됨(오전과 결정적 차이). 잔여 수술: place tx 슬림화·컨슈머 병렬화·워커 델타 배치 (위 플랜 ①③⑤).
   - **수술 ①②③+풀30 적용 후 최종 실측 (matrix v3, 17:11~17:47, 미러 TPS5/depth10 병행)**: SPOT 100 TPS = **에러 0·타임아웃 0으로 완주** (p50 198ms/p95 1.3s — 미러 churn 동반 기준). SPOT 200 = 접수는 유지(피크 ~280 write/s)하나 **부하 중 정산 전이가 33/min까지 기아**, 부하 종료 즉시 88/s 회복 (풀 타임아웃은 2건뿐 — 풀30 유효, 붕괴·유실 없음). **균형 실시간 상한(미러 동반) ≈ 100~150 TPS** — "300 TPS" 목표는 현 단일 프로세스 구조에선 미달. 남은 병목 = Node 1프로세스 CPU + 부하 중 컨슈머 기아 + Wallet 컨보이(락 대기 148샘플 잔존). 다음 사다리: 정산 프로세스 분리 → lastFillOf 제거 → 백프레셔 → MM 계정 분리 → **ADR-069 (인메모리 원장 — 구조 해법)**. (order / settlement / bots-mm)

24. **[✅ FIXED 2026-07-15 — 흉터 잔존] 엔진 matcher — 종결 주문 id의 중복 NO가 재매칭됨 (09:07 정합성 FAIL로 검출, AUDIT 포렌식 확정)**
   - **수정 (09:30, REPROFIX)**: orderbook에 종결 id bounded FIFO 기억(레인당 50k) + matcher 가드 `contains OR was_terminated` → 중복 NO는 REJECTED. pytest RED 4종 → 전체 53 green. **검증 (10:01)**: 수정 엔진으로 25분 미러 churn 후 재측정 — **신규 불일치 0** (기존 흉터 값이 소수 8자리까지 무진행). 잔여 리스크: 재기동 straddle 갭(종결 기억이 스냅샷 미포함 — 종결이 스냅샷 이전·중복이 이후면 뚫림; 실측 재발 시 소용량 지속화 게이팅). 잔존 흉터(아래 원 기록의 taker −77.81 등)는 소급 보정 대상 — reset이 최선. — `matcher.py:38`의 중복 가드 `book.contains(taker.id)`는 **resting 중인 id만** REJECTED 처리. IOC/완전체결로 북에서 사라진 id에 중복 NO가 도달하면(producer 재시도/WAL replay 겹침) **fresh taker로 재매칭**되어 초과 체결 발행. 실증: 주문 949f76b6 (IOC, orig 0.06) — 생성 1h45m 후 중복 NO로 재매칭되어 exec 0.1191 > orig (팬텀 체결 0.0591, 별개 maker·별개 sourceKey라 BE 멱등이 못 막음 — BE는 같은 trade 재발행만 스킵 가능, 로그로 확인). 하류로 taker-bot USDT locked **−77.81** (팬텀 정산이 락에 없는 104.56 차감 — 산술 정확 일치). 06-22의 resting-중복 멱등, 07-14 오전의 REJECTED 발신 수정에 이은 3번째 구멍 — **수정: 종결(FILLED/CANCELED/EXPIRED) id를 TTL/LRU로 기억해 중복 NO를 REJECTED로 반려** + pytest 재현("완전체결 taker에 중복 NO → 재매칭 없음"). 관찰 #23 수술 ①②③은 이 건에서 **무죄 판정** (배치 워커: 중복 sourceKey 0·전량 1회 APPLIED 구조 증명, place tx: locked 증가만·taker 비팬텀 회계 완벽). (match engine)

26. **[아키텍처 관찰] BE 처리량 천장의 2대 = Node 이벤트루프 단일코어 (2026-07-16, ADR-069 S2 후 DIAG 계단 실측)** — Wallet 행 락 소멸(ADR-069 S2) 후 병목이 이동: be-spot Node 프로세스가 **60 TPS에서 이미 CPU ~100%**, 원시 완주 상한 210~240/s, 유효(레이턴시 건전) 용량 100~120 TPS, 500 오퍼 시 혼잡붕괴(달성 81). pg(≤82%)·Prisma 풀(유휴 25+)·엔진(3~4%)은 전부 여유 — 루프를 먹는 것은 요청당 Prisma 왕복 13~15회의 JS 비용 + 동기 HMAC + (수정 전) 테일러/프로젝터 세금. 인증 캐시·테일러 2단 fetch·프로젝터 배치화 적용 후에도 단일코어 상한 자체는 잔존. **해법 사다리 = M1 정산·컨슈머 프로세스 분리(루프 밖으로) → P4 API 수평 확장(원장 소유권 샤딩 선행)** — docs/specs/tps-and-failover-plan.md §5, 캠페인 상세: test-reports/2026-07-16-adr069-ledger-s2-campaign.md. (process topology)

25. **[미확정·재발 0] maker-bot USDT locked 과다 방출 −1,496.99 (동일 세션 검출; #24 수정 후 25분 churn에서 재발 0 — #24의 maker측 발현이었을 개연성 상승, 관찰 유지)** — 종결 주문들이 순합으로 락을 과다 방출해 locked=−54.31 (기대 +1442.68 = 유일 OPEN 주문의 정당 락). 개별 주문 단위 `Σ체결 > 배치락` 0건, DUST leg 57,742건 전수 `lockedDelta==−balanceDelta` 정합 — 단일 주문 초과소진이 아니라 집계 수준에서만 관측됨. 후보: #24의 maker측 변형, 또는 취소/replace 락회계 기존 경로. **placement 락이 저널 없는 직접 UPDATE라 DB 재구성 불가** — ADR-069(락 이벤트소싱)가 이 관측 불가를 해소. 그 전 임시 진단: place/cancel 락 델타 로깅 후 미러 재현. (order / settlement)

19. **[✅ FIXED 2026-07-12] KRW 호가단위가 실제 Upbit보다 거침 (미러 충실도 프로브 검출)** — `krw-ticks.ts`(+ BE `libs/shared/.../krw-tick.ts`)의 [1,000~10,000) 구간 틱이 **5 KRW**인데 실제 Upbit은 **1 KRW**(fidelity 프로브: 실제 스프레드 6.7bps=1KRW vs 로컬 33.5bps=5KRW). 이 팽창 스프레드가 삼각 차익거래 −13~−17bps 장벽의 근원이자 스캘핑의 유일 수익 지점이었음.
   - **수정**: 유저 지시로 두 테이블을 **docs.upbit.com 공식 표**로 교체(라이브 오더북 구간별 실측 교차검증): [1k,5k)→1, [5k,10k)→5, [50k,100k)→50, [100k,500k)→100, [500k,1M)→500, ≥1M→1000 + 저가 꼬리 확장(최소 0.00000001). BE spec 갱신(6/6 pass). 엔진은 틱 미강제(BE 검증)·FE는 exchange-info 런타임 소비라 두 파일이 전부. minNotional 5000 KRW는 이미 실제와 일치.
   - **적용 후 fidelity**: USDTKRW 스프레드 6.7/6.7bps(로컬/실제 일치), 전 심볼 |mid 드리프트| 0.0bps, depth 96%, 김프 Δ≤0.1bps. 팽창-틱 메이커 수익 지점 소멸 확인은 같은 날 위험수익 전략 배터리 참조. (krw-ticks / fidelity)

27. **[심각·해결 2026-07-16 이후] exchange.sh가 BE 프로세스를 못 본다 — 재빌드해도 구 프로세스가 계속 서빙 (2026-07-16 FIX-1 배포에서 실측)** — kill/탐지 패턴은 `bitshuriken-prod-be/dist/apps/(spot|futures|portal)/main`(절대경로 포함)인데 실제 argv는 cwd 기준 상대경로 `node dist/apps/spot/main` → **start가 구 BE를 종료하지 못하고**, 새 `start:prod`는 포트 바인드 실패로 조용히 죽고, 검증 단계는 구 프로세스의 HTTP 200으로 통과(exit 0) = **"BE 코드 반영은 start 재실행" 보장이 깨져 있음**. stop/status도 동일 패턴이라 BE에 blind. 실측: 첫 start 후 11:41 빌드가 계속 서빙, dist mtime vs 프로세스 시작시각 대조로 검출, 수동 SIGTERM 후 재-start로 해소. 수정: 패턴을 실제 argv에 맞추거나 pid 파일 기반으로 전환 + "dist mtime > 프로세스 시작시각이면 경고" 자가검증 추가. (scripts/exchange.sh)
   - **✅ 해결**: exchange.sh의 탐지·종료 패턴이 argv와 같은 상대경로 `dist/apps/(spot|futures|portal|settle)/main`으로 바뀌었고, 포트 5101~5104 리스너 강제 종료 + PID 파일 기반 `assert_be_listener`(포트를 잡은 pid가 이번에 스폰한 자손인지 확인)로 구 프로세스 서빙을 검출한다. 'dist mtime > 프로세스 시작시각' 자가검증은 미구현.

28. **[도구·미해결] loadtest.ts 셧다운 드레인 무한 행 — 측정 오염 유발 (2026-07-16 실측)** — `shutdown()`의 `cancelAllFor`/`openOrders` await(~:424/:431)에 per-request 타임아웃이 없어 소켓 하나가 stuck이면 프로세스가 영구 행(유휴 2.4% CPU) → aggregate JSON 미출력 + 강제 종료 시 잔여 오픈 주문(~320건 실측)이 다음 스텝을 오염(240 스텝 즉시 에러 171). 수정: 드레인 취소에 `AbortSignal.timeout` + 전체 셧다운에 상한 시간. 그때까지 운영 수칙: 스텝 간 잔여 오픈 주문 0 확인을 드레인 조건에 포함. (bots/loadtest)

29. **[기동·미해결] BE spot/futures가 부트 중 죽는다 — `LedgerBaseliner.baseline` 인터랙티브 tx가 Prisma 기본 5s 타임아웃 초과 (2026-09-16 `exchange.sh start` 실측, P2028)** — `BalanceJournal` 1,758,528행에 인덱스가 `@@index([userId, assetSymbol])` 하나뿐이라 부트 마커 조회 `findFirst({marketType, kind: BASELINE})`가 **병렬 seq scan**(EXPLAIN ANALYZE 1.36s, 585,991행/워커 제거)이고, 같은 tx 안의 `findFirst({marketType})`도 스캔. 4개 앱이 동시에 부트하며 같은 스캔을 돌리고 다른 CPU 부하(빌드·학습)가 겹치자 spot 5,062ms / futures 6,126ms → `Transaction already closed` → `onApplicationBootstrap` 실패로 프로세스 종료, start는 `be-spot dead`로 FAIL. **저널이 자랄수록 부트가 느려지는 구조**라 재발 확정적. 수정 후보: (a) `@@index([marketType, kind])` 추가(마이그레이션 1회 — 유저 승인), (b) 마커 조회를 tx 밖으로 빼고 tx는 createMany만, (c) `$transaction(..., { timeout })` 상향. 그때까지 운영 수칙: 다른 CPU 작업 중에 start 금지, 실패 시 재시도. (be / ledger bootstrap)

30. **[엔진·미해결] quote-driven MARKET BUY + FOK가 PARTIAL로 종결 (2026-09-16 테스트 이식 중 발견)** — `_has_full_liquidity`가 quote→base 환산에 stepSize floor를 적용하지 않아 '충분' 판정 후 매칭이 dust(1 step 미만)를 못 채우고 status `P`로 끝난다(예: ask 100×1, `oqq=50.05` → 0.5 체결, 0.05 dust). FOK는 FILLED/REJECTED여야 한다. 실행 가능한 명세: `bitshuriken-prod-match/tests/scenarios/market/test_market.py::test_quote_fok_never_ends_partial`(strict xfail — 엔진 수정 시 마크 제거). 수정 후보: 사전 합산에서 step floor 적용, 또는 dust만 남으면 FILLED 판정. BE spot이 MARKET FOK를 노출하는지 확인 필요. (match engine)

31. **[엔진·latent] combined config 모드(spot+futures 한 프로세스)에서 lane 유실·부트 워터마크 충돌** — `LaneRegistry.__init__`이 `{symbol: lane}`으로 키를 잡고(`engine/lane.py`, `load()`는 중복 가드가 있는 `add()`를 안 씀) `config/tickers.json`은 양 마켓에 같은 심볼 47개가 중복(futures 블록이 뒤) → `registry.get("BTCUSDT")`가 futures lane을 돌려줘 spot 주문이 futures 책에서 매칭된다. `boot_hw`도 partition만 키라 `match.spot.in[p]`/`match.futures.in[p]`의 HW가 서로 덮어씀. 프로드·exchange.sh는 분리 실행(`MATCH_CONFIG_PATH`)이라 latent. 수정: registry 키 `(market, symbol)`, `boot_hw` 키 `(topic, partition)` + 회귀 테스트, 또는 combined 모드를 문서에서 제거. (match engine)

32. **[FE·경미] 지갑 개요가 선물 잔고 조회 실패를 0으로 표시** — `wallet/page.tsx`의 `futuresState`는 `isLoading`만 보므로 `useFuturesBalances`가 오류면 'ready'로 넘어가 선물 합계 0·총합 = spot만 표시되고 오류 표시가 없다. be-futures 다운 시 순자산 과소 표시. ADR-076 §6-⑨(GA 종료 조건). 수정: 쿼리 `isError`를 상태에 반영해 '—'와 오류 문구. (fe / wallet)
