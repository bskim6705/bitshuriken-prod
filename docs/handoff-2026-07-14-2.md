# 새 세션 부트스트랩 #2 (2026-07-14 17:40 기준 — 저녁 세션 핸드오프)

> 첫 메시지에 이 파일 경로만 주면 됨: "docs/handoff-2026-07-14-2.md 읽고 시작해". (오전 핸드오프 docs/handoff-2026-07-14.md는 이 문서로 대체됨)

---

## 붙여넣기용 프롬프트

```
bitshuriken-prod에서 이어서 작업한다. docs/handoff-2026-07-14-2.md를 읽고 상태를 파악한 뒤 시작해.

핵심 컨텍스트:
- 오늘 저녁 세션 대사건: BE 처리량 붕괴 실측·규명(관찰 #23) → 수술 ①②③ + 인덱스 + 풀 상향 적용 → 부하테스트로 검증 중이었음. DB는 17:42 exchange.sh reset으로 완전 초기화됨 (과거 캠페인/체결 데이터 전부 소거 — 유저 지시).
- 진행 중이던 것: 매트릭스 v3 (spot 100/200/300/400 + futures 100/200/300, 미러 병행) — 결과 파일 경로는 핸드오프 §진행중 태스크 참조. check-integrity F1~F4 최종 게이트는 아직 미실행.
- 방향 확정: ADR-069 (잔고 인메모리 원장 + 이벤트소싱, 섀도 모드 마이그레이션) Draft 작성됨 — §6 정책 포인트 유저 확정 대기.
- 하드 룰: 커밋/푸시 금지(지시 시만), prisma migrate 금지(유저 몫), BE의 타 에이전트 미커밋(DLQ/ADR-068) 무접촉, 재기동은 "미러 정지+PENDING=0" 창에서만, reset 후엔 반드시 핫픽스 인덱스 재생성(§운영 레시피).
- 운영 모델: 메인=오퍼레이터(기획·선별·검증·원라이너), 실행은 Opus 포드 다수 병렬. 포드 브리핑 표준은 오전 핸드오프 §포드 운용 참조 (여전히 유효).
```

---

## 이 세션에서 일어난 일 (시간순)

1. **EVO-9 유전 캠페인** (12:05~14:35, G1~G4, 30분 주기로 단축): 16슬롯×500. boxharvest(EVO-8 신작)가 챔피언 계보 (MUB +1.54% 4세대 연속 절대+), DEXE 언와인드에 사다리 6슬롯 물림. 킬 2·이민 3·지터 1. **최종 −172.33/8,000 (−2.15%)** — 인프라 사고·수술로 16:40 강제 마감 (상세: docs/trading/journal.md 최신 항목).
2. **정산 붕괴 사고** (14:14~14:29): 미러 churn이 지속 상한 초과 → 처리량 60→2.4 TPS 붕괴, NEW 8k. 미러 SIGINT로 해소. **분단위 재구성 + 합성 부하테스트로 근본 원인 확정** (유저 가설 적중):
   - 주범: **Wallet 행 락 컨보이** — place tx가 락 쥔 채 다중 왕복(idle-in-transaction 현행범 pg_locks 108건 캡처), 단일 MM 계정이라 전 심볼 직렬화, 락 대기가 Prisma 풀(당시 17)을 태워 전면 마비.
   - 공범: **lastFillOf 무인덱스 O(백로그) 역스캔** (728ms 실측) × 직렬 컨슈머 → 자기강화 되먹임.
   - 전부 **관찰 #23**에 증거·수치와 함께 기록됨 (docs/specs/refactor-observations.md).
3. **수정 적용** (전부 미커밋):
   - Trade (makerOrderId)/(takerOrderId) **인덱스** — DB 핫픽스(`*_hotfix_idx`) + schema.prisma @@index 2줄. 728ms→1.3ms.
   - **수술 ①** place tx 슬림화 (락 홀드 왕복 3→1, order 먼저→원자 차감 RETURNING): spot `order.service.ts` + futures `margin.service.ts`.
   - **수술 ②** 컨슈머 파티션 병렬화 (`partitionsConsumedConcurrently`, 파티션 헬퍼 사용): spot/futures `main.ts`.
   - **수술 ③** 정산 워커 델타 배치 (fast path 배치 1 tx + 실패 시 기존 per-event 전체 폴백, DLQ 시맨틱 보존): spot `settlement.worker.ts`. **futures는 NO-GO** (avgEntryPrice·flip·RPNL이 순서 의존 — 배치 시 회계 파손. 정직 게이트).
   - **풀 상향**: BE .env DATABASE_URL에 `?connection_limit=30&pool_timeout=20` (백업 .env.bak-pool).
   - 신규 spec: order.service (슬림 tx 3), settlement.worker.batch (12), margin.service (6). **전체 jest 314/315** (유일 실패 = 타 에이전트 미커밋 코드의 기존 실패 — stash A/B로 증명됨, futures-trading.service.spec 생성자 인자 10 vs 8).
4. **부하 도구 자산화*: `bots/src/loadtest.ts` (신규) — 계정 N·토큰버킷 TPS·place/cancel churn+self-cross·p50/p95 계측·정리 검증. `LT_MARKET=FUTURES` 모드 포함 (futures는 mark price 필요 — 관찰 #20 재확인, 밴드 ±5%).
5. **부하 실측** (수술 전→후):
   - 수술 전: 접수 상한 ~200 TPS(계정16+인덱스), 100부터 p95 스파이크, 부하 중 정산 기아(전이 ~1/s), 드레인 87/s.
   - 수술 후(풀17): spot 100 클린 / 200에서 풀 고갈(NEW 43k) — 단 **드레인 235/s(2.7배) + 43k 전량 무유실 정산** (컨슈머 에러 시 오프셋 미커밋·재처리 실증 = "밀려도 잃지 않음").
   - 수술 후(풀30): 매트릭스 v3 진행 중이었음 (§진행중 태스크).
6. **exchange.sh 버그 2종 발견** (미수정, 스크립트 계약 위반): ① `start`가 이미 떠 있는 BE dist를 재시작하지 않음 (serving 200이면 통과 — 코드 반영 안 됨!). ② stop/reset의 정리가 BE dist 프로세스를 못 잡아 좀비 생존 → 새 Kafka에 토픽 1-파티션 auto-create 유발 (reset 실패 재현됨). **우회법: `ps aux | grep dist/apps`로 PID 확인 후 수동 kill → start.**
7. **ADR-069 Draft 작성**: 잔고 인메모리 원장+이벤트소싱, 엔진 패턴(WAL+스냅샷+리플레이) 이식, 섀도 모드 마이그레이션, F1~F4 재정의. **§6 정책 6개 유저 확정 대기.** 엔진은 이미 동형 아키텍처로 강건함을 코드로 확인 (main.py:6-8, snapshot_store.py). 최대 난제 = 엔진↔원장 경계의 정확히-한-번 (관찰 #21이 산 증거).

## 현재 스택 상태 (17:40)

| 항목 | 상태 |
|---|---|
| DB | **17:42 reset으로 프레시** (tickers 106 시드). 핫픽스 인덱스 2개 재생성됨. 과거 데이터 없음 |
| BE ×3 | **수술판 + 풀30** 가동 (PID 17:09 생성 — 재기동 시 §레시피의 좀비 체크 필수) |
| 엔진 ×2 | 수정판 matcher (REJECTED 발신) 가동 |
| 미러 bots | **가동 중** (TPS5/depth10/rec600, 9심볼 — 프레시 DB라 ALLO 재수렴 성공) |
| agents | 0 (EVO-9 함대는 reset 전에 전량 정지·기록 완료. agentd도 다운 상태일 것) |
| 진행중 태스크 | 매트릭스 v3: `/private/tmp/claude-501/-Users-kitsune-bitshuriken-bitshuriken-prod/115634f2-a224-45c1-968e-4c1dfe3dd24b/tasks/bi83739nk.output` (세션 죽으면 태스크도 죽음 — 파일은 남음). DB 샘플러 CSV: 같은 경로 `scratchpad/loadtest-samples.csv`, 락 캡처 `loadtest-locks.log` |

## 미커밋 인벤토리 (레포별, 소유 구분)

- **bitshuriken-prod-be** — 이 세션 것: `order.service.ts`(수술①) `main.ts`×2(②) `settlement.worker.ts`(③) `margin.service.ts`(futures①) `schema.prisma`(@@index 2줄) `.env`(풀30, 백업 .env.bak-pool) + 신규 spec 3파일 + order.service.spec 확장. **타 에이전트 것 (무접촉!)**: DLQ/ADR-067 (settlement.worker 일부·dlq), ADR-068 MM-cap (order.service 일부·futures-trading.service — 기존 spec 실패 1건 이쪽), kline intervals, krw-tick, order-list, seed.
- **bitshuriken-prod-bots**: `loadtest.ts`(신규, 이 세션) + exchange.ts 타임아웃(오전) + fidelity/dlq(기존).
- **bitshuriken-prod-match**: matcher.py 수정+테스트 48green (오전).
- **bitshuriken-prod-agents**: 전략 16종+선물배관 (오전 이전).
- **docs**: ADR-069(신규), 관찰 #23(신규), 핸드오프 2종, journal/lessons 갱신.

## 열린 항목 (우선순위 — 07-15 09:40 갱신)

0. **[완료됨 07-15 오전]** 매트릭스 v3 판정: SPOT 100 = 에러 0 완주 (p50 198ms, 미러 동반), SPOT 200 = 접수 유지·부하 중 정산 기아(33/min) → **균형 실시간 상한 ≈ 100~150 TPS** (관찰 #23 말미 기록). check-integrity 실행됨 → **FAIL 3종** → AUDIT 포렌식으로 **수술 ①②③ 무죄, 엔진 유죄 확정** (관찰 **#24**: 종결 id 중복 NO 재매칭 → 초과 체결·락 파손; **#25**: maker 락 과다방출 −1,497 미확정 — ADR-069 저널링이 관측 수단).
1. **[완료 07-15 10:01] 엔진 #24 수정·검증 종결** — 종결-id FIFO(50k)+REJECTED, pytest 53 green, 엔진 재기동(09:33) 후 25분 churn 재검증: **신규 불일치 0** (기존 흉터만 잔존 — 소수 8자리 무진행). #25도 재발 0 (#24 maker측 발현 개연성). 잔여: 재기동 straddle 갭 (관찰 #24 기록).
2. **흉터 정리** — 잔존 FAIL 3종은 어제의 역사적 오염 (활성 아님). 다음 작업 전 reset 권장 (인덱스 재생성 잊지 말 것) 또는 기지 기준선으로 유지. 미러는 현재 정지 상태.
3. **Prisma 마이그레이션 (유저)**: `cd bitshuriken-prod-be && npx prisma migrate dev --name trade-orderid-indexes` — 단 schema.prisma에 타 에이전트 DLQ 변경(+20줄)이 물려 있어 조율 후 실행. 실행 전까지는 **reset마다 핫픽스 인덱스 재생성 필수** (§레시피).
4. **ADR-069 §6 정책 확정 (유저)** → 섀도 모드 설계 착수. 병행 가능한 살아남는 수술: 정산 프로세스 분리 / lastFillOf 제거 / 백프레셔 / MM 계정 분리 (ADR-069 문서 참조).
5. exchange.sh 버그 2종 수정 (BE 미재시작 / 좀비 미정리).
6. 관찰 #21 (정산 오프셋 원자성 — 타 에이전트 영역, ADR-069 §1이 구조 해법).
7. futures 정산 워커 성능 (③ NO-GO였으므로 per-event 유지 — futures 부하 결과 보고 판단).

## 운영 레시피 (이 세션에서 실증된 것)

- **reset 후 필수**: `docker exec bitshuriken-prod-postgres-1 psql -U bitshuriken -d bitshuriken -c 'CREATE INDEX IF NOT EXISTS "Trade_makerOrderId_hotfix_idx" ON "Trade"("makerOrderId"); CREATE INDEX IF NOT EXISTS "Trade_takerOrderId_hotfix_idx" ON "Trade"("takerOrderId")'`
- **재기동 절차**: ① `ps aux | grep dist/apps` 좀비 확인·kill ② 미러 정지(pkill -INT -f src/run.ts) + PENDING=0 확인 ③ exchange.sh start ④ **새 PID인지 반드시 확인** (버그 때문에).
- **부하테스트**: `cd bitshuriken-prod-bots && LT_MARKET=SPOT|FUTURES LT_ACCOUNTS=16 LT_TPS=200 LT_DURATION_S=180 LT_CROSS_PCT=20 npx tsx src/loadtest.ts`. 심볼 기본 BTC/ETH. 판정: in-flight 포화로 달성<목표 = 상한 도달.
- **정산 건강 쿼리**: stuck NEW `select count(*) from "Order" where status='NEW' and "createdAt" < now() - interval '2 minutes'` — 500+ 성장 시 유입 차단이 정답 (드레인은 빠름).
- 락 컨보이 관측: pg_stat_activity에서 `wait_event_type='Lock'` + `pg_blocking_pids()` — 캡처 스크립트는 scratchpad/loadtest-samples 참조 (새 세션에선 재작성 필요).

## 트레이딩 지식 갱신

- journal.md에 EVO-9 항목 추가됨 (최신 맨 위). lessons.md L18(boxharvest 계보)·L19(킬 룰 레짐 보정) 추가됨. L15(구조=엣지)는 하락 테이프에서도 재확인 (randladder −5.03% > crashcatch tp0.012 −5.88%).
- EVO-10 재개 시: 오전 핸드오프의 캠페인 레시피 + 이 세션의 킬 룰 보정("2연속 음수알파 AND 창 절대 PnL≤0") 사용. DB가 리셋됐으므로 미러 워밍부터.
